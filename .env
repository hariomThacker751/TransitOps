# Mock mode simulates the full backend (rules engine, state machine, KPIs) in the browser.
# Set to "false" and provide VITE_API_BASE_URL to use the real Express + MySQL backend.
VITE_USE_MOCK=true
# VITE_API_BASE_URL=http://localhost:4000
